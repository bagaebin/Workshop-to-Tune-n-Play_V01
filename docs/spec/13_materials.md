# §13 더미 재료

> **정본** — [SPEC.md](../../SPEC.md) §13
> **근거** — 구현 명세 V0.3 §11-2 · 기능 명세 G4 · T11 · D13
> **대응 코드** — `materials/sound.json` · `materials/img/i1..i5.png` · `src/ops/material.ts`
>
> 이 파일은 스펙을 다시 쓰는 자리가 아니라 **이 절을 구현하며 쌓이는 것**(메모 · 구현 결정 · 교정값 · 상위 문서에 되돌릴 정정)을 두는 자리다. 규칙·수치가 SPEC과 어긋나면 SPEC(볼트 원본)을 먼저 고친다.

## 이 절이 정하는 것

- 소리 — `materials/sound.json` `{ "vals": [ {on,pitch,len,vel,tone} × 5–8 ] }` · `on`은 0부터 상대값 · 실물도 같은 JSON, 코드 변경 없음
- 이미지 — `materials/img/i1..i5.png` · 단색 사각형 · **1200 × 800**(3:2) · 번호만 · 실물도 같은 파일명·규격
- 로딩 없음(N1) — JSON과 이미지를 첫 페인트 전에 디코드

## 열린 항목

- [ ] **T11** 실물 교체 — **09.25 저녁까지.** 미완이면 더미로 파일럿
- [ ] 이미지 선정 지침(기능 명세 G4 — 다축 · 축 검산표 · 미완성도 · 한 단어 탈락 · 금지 넷 · 후보 10장 이상)은 볼트에서 관리. 저장소에는 파일만
- [ ] T16 이미지 배치 편향 통제 — 파일럿

## 구현 메모

- 09.22 (3단계) — `sound.json`은 `import`, 이미지는 `import.meta.glob('../../materials/img/*.png', {query:'?url'})`로 번들에 들어가 서비스 워커가 함께 프리캐시한다. 실물 교체 = 파일 덮어쓰기 + 재빌드
- 09.22 — 더미 이미지는 `tools/make_dummy_materials.py`(단색 + 숫자). 첫 페인트 전에 `decode()`

## 상위 문서에 되돌릴 것

_(없음)_
