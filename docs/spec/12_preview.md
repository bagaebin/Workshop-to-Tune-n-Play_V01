# §12 미리보기

> **정본** — [SPEC.md](../../SPEC.md) §12
> **근거** — 구현 명세 V0.3 §11-1 · 기능 명세 N5 · K2 · K4
> **대응 코드** — `src/preview.ts`(같은 렌더러 `preview` 모드) · `src/render.ts`
>
> 이 파일은 스펙을 다시 쓰는 자리가 아니라 **이 절을 구현하며 쌓이는 것**(메모 · 구현 결정 · 교정값 · 상위 문서에 되돌릴 정정)을 두는 자리다. 규칙·수치가 SPEC과 어긋나면 SPEC(볼트 원본)을 먼저 고친다.

## 이 절이 정하는 것

- 좌 6 · 서랍 3 슬롯 안 미니 캔버스 · 하나의 `requestAnimationFrame` · 무음 · 루프 `PREVIEW_LOOP` 2 500 ms ☐
- 아홉의 길이·움직임 양 동일 · 손가락·경로 없음 · **결과만**
- grid 눈금 나타났다 사라짐 · gen.hand 점 하나 · gen.rule 등간격 열 · gen.random 불규칙 열(시드 `hash(seed, loopIndex)`) · play 헤드 · all 한꺼번에 밝아짐 · mat.blank 커서+획(칩 있으면 칩) · mat.sound 점 덩어리 · mat.image 사각형
- 절단(#3) 시 각 슬롯 마지막 프레임 한 장으로 대체

## 열린 항목

- [ ] **절단 후보 3순위** — 09.24 밤 판정 → 정지 프레임
- [ ] 미리보기 렌더가 오디오 그래프·입력 처리에 물리지 않게 격리

## 구현 메모

- 09.22 (3단계) — 9종 모두 같은 `appear` 곡선(0.2 나타남 · 유지 · 0.15 사라짐). 규칙·난수는 왼쪽부터 순서대로, 난수는 `hash(seed, loopIndex)`. 정지 프레임 절단은 헤더 `cuts`에 `preview_loop`가 있으면 `PREVIEW_STATIC_PHASE`(0.6) 고정
- 09.22 — 미리보기는 `render.ts`의 슬롯 루프 안에서 같은 rAF로 그린다. 오디오와 무관

## 상위 문서에 되돌릴 것

_(없음)_
