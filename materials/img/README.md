# materials/img/

`i1.png` … `i5.png` — **1200 × 800 px (3:2)**. 더미는 단색 사각형에 번호만. 실물도 같은 파일명·규격 (T11 · 09.25 저녁).

더미는 `python3 tools/make_dummy_materials.py`로 만든다(09.22 생성). 실물로 바꿀 때는 파일만 덮어쓴다 — 코드 변경 없음.
