# materials/ — 재료 (SPEC §13)

| 재료 | 파일 | 형식 | 상태 |
| --- | --- | --- | --- |
| 소리 | `sound.json` | `{ "vals": [ {on, pitch, len, vel, tone} × 5–8 ] }` · `on`은 0부터 상대값(ms) · `pitch·vel·tone` 0–1 | **더미** |
| 이미지 | `img/i1.png` … `img/i5.png` | 1200 × 800 px (3:2) · 실물도 같은 파일명·규격 | **없음** — 더미 생성 전 |

- 실물 교체 기한 **09.25 저녁 (T11)**. 실물도 같은 파일명·같은 JSON이라 코드 변경 없음
- 개수(이미지 5) · 종횡비(3:2)는 지금 고정 — 바뀌면 `image.*` 좌표가 참여자 간 비교 불가
- 로딩 없음(N1) — JSON과 이미지는 첫 페인트 전에 디코드해 둔다
- 이미지 선정 지침은 볼트 기능 명세 G4에 있다. 저장소에는 파일만 둔다
