{
  "name": "workshop-to-tune-n-play",
  "version": "0.0.0",
  "private": true,
  "description": "Sound by Scratch — digital probe (SPEC.md). 외부 런타임 라이브러리 0.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "chain": "python3 tools/chain.py",
    "dev:host": "vite --host --port 5173 --strictPort"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "@types/node": "^22.0.0"
  }
}
