# 자가 파일럿 진행 절차 — 09.26 (SPEC §14-1 · §15 · 세션 계획 V03 §11)

목적 셋 — ① §15 수용 테스트를 실기기에서 ② 상수표 ☐ 교정 ③ **두 잠금식 비교(D14)**. 2회 돌린다. 1회는 `v0.3`, 2회는 `v0.2`.
로그 검사와 교정 수치는 전부 `tools/chain.py`가 뽑는다. 사람은 옮겨 적고 판단만 한다.

## 0. 전날 (09.25 밤)

- [ ] `materials/sound.json` · `materials/img/i1..i5.png` 실물로 교체(T11) → `npm run build`. 못 했으면 더미로 진행하고 T11을 `pilot-calibration.md`에 적는다
- [ ] 절단 판정 — 09.22 기준 **자른 것 없음**(`CUTS = []`). 파일럿에서 마이크 입력 채널이 헛노트를 내거나 게이트가 재생을 막으면 `src/constants.ts`의 `CUTS`에 `'mic_input'`을 넣고 다시 빌드한다. 다른 후보(이미지 크기 · 미리보기 루프 · 슬라이더 · 라벨)는 같은 방법
- [ ] 배포 — main에 push하면 GitHub Pages에 올라간다: <https://bagaebin.github.io/Workshop-to-Tune-n-Play_V01/> (Actions 탭에서 `pages` 성공 확인). iPad Safari에서 열어 **홈 화면에 추가**. 한 번 띄워 서비스 워커 캐시를 굳힌다. 이후 기내 모드에서 다시 떠야 한다. 헤더 `build`의 해시가 마지막 커밋과 같은지 확인
- [ ] 마이크 — standalone 앱에서 시트 [마이크 권한 요청]을 한 번 허용해 둔다(N1). iPadOS 설정 › Safari › 마이크 허용 확인

## 1. 회차마다 (약 30분)

| # | 할 것 | 확인 |
| --- | --- | --- |
| 1 | 기내 모드 · 회전 잠금 · 무음 모드 해제 · 볼륨 70 % | 👁 |
| 2 | 앱 실행 → 오른쪽 아래 빌드 식별자가 마지막 커밋인지 확인 → 화면 왼쪽 위 구석(80 pt 안)을 5연타 **또는 3초 길게 누르기** → 시트: `pid` `PILOT1`(2회는 `PILOT2`) · 마이크 권한 · 레벨 미터로 임계 조정 · 체크리스트 전부 · **2회만 「원식 v0.2를 실제 잠금으로」 체크** | 시트 |
| 3 | 카메라 1대 녹화 시작(시계 확인) · Guided Access 시작 · [시작] | 플래시가 카메라에 찍혔는가 |
| 4 | 튜토리얼 원 — 탭 · 누르기 · 끌기 · 두 손가락 · 가장자리 스와이프 | N4 같은 소리 · N2 기록됨 |
| 5 | 시트 → [구간 1 시작]. **§15 G 항목을 순서대로** 한 번씩 해 본다 — 탭/누르기/끌기 · 고치기 · 지우기(밖으로) · 선택 one/many/all · 격자 · 재생/시작점 탭/부분 반복 · 소리 재료 열람/채택 · 이미지 패널/놓기/손잡이 · 적기(짧은 글로 abort · 문장 · 칩을 면과 띠에) · 규칙/난수/슬라이더 · 마킹 · 남기고/지우고 새로 · 되돌아가기 · 흥얼거림 | 👁 |
| 6 | 8분에 「여기까지」가 3초에 걸쳐 나타나는가. 1회는 **직접 눌러** 끝내고, 2회는 **15분 상한**까지 둔다 | G11 |
| 7 | 구간 2 — 잠긴 것이 사라졌는가 · 나머지가 움직이지 않았는가 · 사라진 자리를 눌러 본다 · 7분 뒤 회고 모드 | G15 |
| 8 | 회고 시트 — 마킹 재생 · 미사용 목록 · 정지 목록 · 버튼 넷 첫 접촉 · 잠금 근거 | 시트 |
| 9 | [내보내기] → 공유 시트 → 파일 앱에 `PILOT1_2026-09-26.jsonl` + `.audio.m4a` 둘 | 환경 |
| 10 | 새로고침 복구도 한 번 — 구간 1 도중 앱을 껐다 켜서 노트·구간·목록이 돌아오는지 (2회에서만) | resume |

## 2. 로그 검사 (Mac)

```bash
python3 tools/chain.py ~/Downloads/PILOT1_2026-09-26.jsonl --pilot
```

- 위쪽 ⚙ 항목이 전부 ✔인지 → [checklist.md](checklist.md)의 ⚙ 칸을 채운다. 👁 항목은 회차 중 본 것을 적는다
- 아래 「파일럿 교정 수치」를 [pilot-calibration.md](pilot-calibration.md)의 **관찰** 칸에 옮긴다
- 문제가 보였으면 그 자리에서 [pilot-issues/](pilot-issues/README.md)에 `PI-00N` 파일을 하나씩 — 심각도와 **세션 전 필수** 여부를 먼저 적는다
- `[D14]` 줄 — 두 회차 모두 「같은 축·값」이면 D14는 **파일럿에서 갈리지 않음**, 하나라도 「★ 다르다」면 어느 쪽이 *"가장 많이 쓴 것"*에 가까운지 회고 시트의 체류 표와 함께 결정 기록에 적는다

## 3. 교정 반영 순서

☐ 값을 바꾸기로 했으면 **볼트 개발 명세 §2 → `SPEC.md` 복사 → `src/constants.ts`** 순서. `docs/spec/02_constants.md`의 교정 이력에 한 줄. 세션 전원이 같은 빌드여야 하므로 **09.28 예비심사 전에 마지막 빌드**를 확정하고 iPad 캐시를 다시 굳힌다.

## 4. 이날 안 되면

- 재료 실물 없음 → 더미로 진행. T11만 미확인으로 남긴다(G4 진입 비용 · T16은 검증 불가)
- 마이크 입력 채널 이상 → `CUTS=['mic_input']`. 녹음 트랙은 남는다
- 공유 내보내기 실패 → 로그는 IndexedDB에 있다. 시트 [재시도]. 그래도 안 되면 Mac에서 `npm run dev:host`로 열어 `?fast` 없이 복구한 뒤 Shift+F → 내보내기(다운로드 폴백)
