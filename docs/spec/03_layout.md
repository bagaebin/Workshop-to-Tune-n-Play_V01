# §3 화면과 좌표

> **정본** — [SPEC.md](../../SPEC.md) §3
> **근거** — 구현 명세 V0.3 §1 (T1 · ㉑) · 기능 명세 §2
> **대응 코드** — `src/layout.ts`(사각형 · 히트 테스트) · `src/render.ts`
>
> 이 파일은 스펙을 다시 쓰는 자리가 아니라 **이 절을 구현하며 쌓이는 것**(메모 · 구현 결정 · 교정값 · 상위 문서에 되돌릴 정정)을 두는 자리다. 규칙·수치가 SPEC과 어긋나면 SPEC(볼트 원본)을 먼저 고친다.

## 이 절이 정하는 것

- 영역 4 — 재료 서랍 (0,0)–(160,884) · 시간축 띠 (160,0)–(1366,48) · 작업 면 (160,48)–(1366,884) · 하단 띠 (0,884)–(1366,1024)
- 배율 `s = min(vw/1366, vh/1024)` · 중앙 레터박스 · 로그 좌표는 언제나 가상 좌표
- 하단 띠 슬롯 10 — 좌 6 셔플(`grid gen.hand gen.rule gen.random play all`) · 우 4 고정(`mark canvas.keep canvas.discard done`) · x = 40·164·288·412·536·660 / 854·978·1102·1226
- 서랍 — 재료 슬롯 3 셔플 (30,32)(30,168)(30,304) · 규칙 슬라이더 (30,440)–(130,600) 조건부 · 캔버스 목록 (16,640)–(144,884) 2×5
- 이미지 패널 (160,48)–(360,884) · 5장 150×100 셔플 + `absent` 100×100 · 열린 동안 가려진 접촉 `blocked:true`
- 온스크린 키보드 — 입력 칸 1206×80 · 작업 면 좌표계 불변 · 가려진 영역 `blocked:true` · 바깥 탭 = 확정
- 좌표 ↔ 값 — `on = (x−160)/1206·L` · `pitch = 1−(y−48)/836` · `midi = 40 + 48·pitch` · 격자 양자화는 렌더·재생 시에만

## 열린 항목

- [ ] `done` 슬롯이 `SEG1_APPEAR` 전에는 렌더되지 않으면서 자리는 비워 두는 처리
- [ ] 이미지 패널이 열린 동안의 히트 테스트 우선순위(§5-1) 검산
- [ ] `visualViewport` 기준 입력 칸 배치를 iPadOS 26 Safari standalone에서 확인

## 구현 메모

_(비어 있음 — 구현하는 날 채운다)_

## 상위 문서에 되돌릴 것

_(없음)_
